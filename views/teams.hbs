


<div class="container d-flex flex-column justify-content-center align-items-center col-12 col-md-12 col-sm-12 col-lg-8 col-xl-8" >
<h1>All workers list </h1>

<form class="form-inline d-flex justify-content-center" action="/teams" method="GET">
{{!-- <label for="">Search :</label> --}}
    <input type="text" name="search" placeholder="Search....." value="{{search}}"/>
    <input type="hidden" name="sort" value="{{sort}}"/>
    <input class="btn btn-secondary" type="submit" value="SEARCH"/>
    
</form>


<table class="table table-hover table-dark">
    <thead>
        <tr>
            <th class="th">Full Name:</th> 
            <th class="th">Age:</th>
            <th class="th"> Email:</th>
            <th class="th">Options</th>
        </tr>
    </thead>
    <tbody>
        {{#each teams}}
        <tr>
            <td>{{this.teamName}}</td>
            <td>{{this.MyAge}}</td>
            <td>{{this.MyEmail}}</td>
            <td>
                <a  href="/team/?id={{this._id}}"><i class="fas fa-edit text-secondary"></i></a>
                <a  href="/team-delete/?id={{this._id}}"><i class="fas fa-trash text-secondary"></i></a>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>

<div class="boxbutton">

    <div>
        Znaleziono <span class="text-primary"> {{count }} </span> rekord(y/ów)
    </div>
    <div class="sort">
        <div class="sort_sort">
            <a class="" href="/teams?sort=1"><i class="fas fa-sort-alpha-up"></i></a>
    
            <a class="" href="/teams?sort=-1"><i class="fas fa-sort-alpha-up-alt"></i></a>
        </div>
        <div class="sort_sort">
            <a class="" href="/teams?sort={{sort}}&page={{prevPage}}"><i class="fas fa-backward"></i></a>
             {{page}}
            <a class="" href="/teams?sort={{sort}}&page={{nextPage}}"><i class="fas fa-forward"></i></a>
        </div>
    </div>

        <a class="btn btn-secondary" class="btnList" href="/team">Dodaj</a>
</div>
</div>